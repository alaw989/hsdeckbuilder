---
phase: 04-monetization-platform-polish
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - resources/js/Components/Mobile/MobileTabs.vue
  - resources/js/Components/Mobile/HamburgerMenu.vue
  - resources/js/Pages/DeckBuilder.vue
  - resources/js/Layouts/AuthenticatedLayout.vue
  - resources/js/Components/CardTooltip.vue
  - vite.config.js
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Deck builder has tabbed navigation on mobile (Cards/Deck/Analytics)"
    - "Navigation hamburger menu with slide-out drawer on mobile"
    - "Card tooltips show as modal on mobile (tap to open, tap outside to close)"
    - "Images use lazy loading below the fold"
    - "Application is responsive and functional on mobile devices"
    - "Pages load quickly with optimized assets (< 3 seconds)"
  artifacts:
    - path: "resources/js/Components/Mobile/MobileTabs.vue"
      provides: "Tabbed navigation for mobile deck builder"
      min_lines: 50
    - path: "resources/js/Components/Mobile/HamburgerMenu.vue"
      provides: "Slide-out navigation drawer for mobile"
      min_lines: 60
    - path: "resources/js/Components/CardTooltip.vue"
      provides: "Modal-style tooltip for mobile tap interaction"
      contains: " Teleport|isMobile"
    - path: "resources/js/Layouts/AuthenticatedLayout.vue"
      provides: "Responsive layout with hamburger menu"
      contains: "md:hidden|sm:flex"
  key_links:
    - from: "resources/js/Pages/DeckBuilder.vue"
      to: "resources/js/Components/Mobile/MobileTabs.vue"
      via: "MobileTabs import and activeTab state"
      pattern: "MobileTabs|activeTab"
    - from: "resources/js/Components/CardTooltip.vue"
      to: "window.innerWidth"
      via: "Mobile detection for modal vs tooltip"
      pattern: "isMobile|window.innerWidth"
    - from: "vite.config.js"
      to: "import.meta.glob"
      via: "Code splitting configuration"
      pattern: "import.meta.glob"
---

<objective>
Create mobile responsive design with tabbed navigation and performance optimizations.

Purpose: Ensure the deck builder is fully functional on mobile devices with native-app-like UX while maintaining fast page loads through lazy loading, code splitting, and image optimization.

Output: MobileTabs component, HamburgerMenu navigation, modal-style card tooltips for mobile, responsive DeckBuilder layout, and performance optimizations.
</objective>

<execution_context>
@/home/deck/.claude/get-shit-done/workflows/execute-plan.md
@/home/deck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-monetization-platform-polish/04-RESEARCH.md
@.planning/phases/04-monetization-platform-polish/04-CONTEXT.md

# Phase 04-01 Completion
@.planning/phases/04-monetization-platform-polish/04-01-SUMMARY.md

# Existing Components to Modify
@resources/js/Layouts/AuthenticatedLayout.vue
@resources/js/Pages/DeckBuilder.vue
@resources/js/Components/CardTooltip.vue
@resources/js/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MobileTabs component for tabbed navigation</name>
  <files>resources/js/Components/Mobile/MobileTabs.vue</files>
  <action>
Create directory and component:

1. **Create directory:**
```bash
mkdir -p resources/js/Components/Mobile
```

2. **Create MobileTabs.vue:**

```vue
<script setup>
const props = defineProps({
  activeTab: {
    type: String,
    required: true
  }
})

const emit = defineEmits(['change-tab'])

const tabs = [
  { key: 'cards', label: 'Cards' },
  { key: 'deck', label: 'Deck' },
  { key: 'analytics', label: 'Analytics' }
]
</script>

<template>
    <div class="md:hidden fixed bottom-0 left-0 right-0 bg-white border-t border-gray-200 z-40 safe-area-inset-bottom">
        <div class="flex justify-around">
            <button
                v-for="tab in tabs"
                :key="tab.key"
                @click="$emit('change-tab', tab.key)"
                :class="[
                    'flex-1 py-3 text-center transition-colors duration-200 flex flex-col items-center justify-center',
                    activeTab === tab.key
                        ? 'text-blue-600 bg-blue-50 border-t-2 border-blue-600'
                        : 'text-gray-600 hover:bg-gray-50'
                ]"
                :aria-label="tab.label"
                :aria-selected="activeTab === tab.key"
            >
                <svg v-if="tab.key === 'cards'" class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"/>
                </svg>
                <svg v-else-if="tab.key === 'deck'" class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"/>
                </svg>
                <svg v-else class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>
                </svg>
                <span class="text-xs mt-1 font-medium">{{ tab.label }}</span>
            </button>
        </div>
        <!-- Spacer for safe area on iOS -->
        <div class="h-safe-area-inset-bottom"></div>
    </div>
</template>

<style scoped>
.safe-area-inset-bottom {
    padding-bottom: env(safe-area-inset-bottom, 0px);
}
.h-safe-area-inset-bottom {
    height: env(safe-area-inset-bottom, 0px);
}
</style>
```

DO NOT:
- Show tabs on desktop (md:hidden class)
- Use external icons (inline SVGs for zero dependencies)
- Forget safe area insets for iOS devices

RESEARCH NOTE: Per CONTEXT.md, tabbed navigation on mobile with Cards/Deck/Analytics tabs.
  </action>
  <verify>
grep -n "MobileTabs\|activeTab\|change-tab\|md:hidden" resources/js/Components/Mobile/MobileTabs.vue
  </verify>
  <done>
MobileTabs component exists with 3 tabs, icons, safe area insets, and mobile-only visibility.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create HamburgerMenu component for mobile navigation</name>
  <files>
  resources/js/Components/Mobile/HamburgerMenu.vue
  resources/js/Layouts/AuthenticatedLayout.vue
</files>
  <action>
1. **Create HamburgerMenu.vue:**

```vue
<script setup>
import { ref } from 'vue'

const isOpen = ref(false)

const emit = defineEmits(['close'])

function toggleMenu() {
    isOpen.value = !isOpen.value
}

function closeMenu() {
    isOpen.value = false
    emit('close')
}
</script>

<template>
    <div class="md:hidden">
        <!-- Hamburger button -->
        <button
            @click="toggleMenu"
            class="p-2 rounded-md hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-blue-500"
            aria-label="Toggle navigation menu"
            :aria-expanded="isOpen"
        >
            <svg v-if="!isOpen" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
            </svg>
            <svg v-else class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
            </svg>
        </button>

        <!-- Overlay -->
        <Transition
            enter-active-class="transition-opacity duration-300"
            enter-from-class="opacity-0"
            enter-to-class="opacity-100"
            leave-active-class="transition-opacity duration-200"
            leave-from-class="opacity-100"
            leave-to-class="opacity-0"
        >
            <div
                v-if="isOpen"
                class="fixed inset-0 z-50 bg-black/50"
                @click="closeMenu"
            ></div>
        </Transition>

        <!-- Slide-out drawer -->
        <Transition
            enter-active-class="transition-transform duration-300"
            enter-from-class="-translate-x-full"
            enter-to-class="translate-x-0"
            leave-active-class="transition-transform duration-200"
            leave-from-class="translate-x-0"
            leave-to-class="-translate-x-full"
        >
            <div
                v-if="isOpen"
                class="fixed inset-y-0 left-0 z-50 w-64 bg-white shadow-xl"
                @click.stop
            >
                <div class="flex items-center justify-between p-4 border-b border-gray-200">
                    <span class="text-lg font-semibold text-gray-900">Menu</span>
                    <button
                        @click="closeMenu"
                        class="p-2 rounded-md hover:bg-gray-100"
                        aria-label="Close menu"
                    >
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                        </svg>
                    </button>
                </div>

                <nav class="p-4 space-y-1">
                    <a
                        href="/dashboard"
                        class="flex items-center px-4 py-3 text-gray-700 rounded-lg hover:bg-gray-100 transition-colors"
                        @click="closeMenu"
                    >
                        <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
                        </svg>
                        Dashboard
                    </a>
                    <a
                        href="/deck-builder"
                        class="flex items-center px-4 py-3 text-gray-700 rounded-lg hover:bg-gray-100 transition-colors"
                        @click="closeMenu"
                    >
                        <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"/>
                        </svg>
                        Deck Builder
                    </a>
                </nav>
            </div>
        </Transition>
    </div>
</template>
```

2. **Modify AuthenticatedLayout.vue** to use HamburgerMenu:

Replace existing mobile hamburger button with component import:
```vue
<script setup>
import HamburgerMenu from '@/Components/Mobile/HamburgerMenu.vue'
// ... other imports
</script>

<template>
    <!-- Replace existing hamburger button section with: -->
    <div class="-me-2 flex items-center sm:hidden">
        <HamburgerMenu />
    </div>
</template>
```

DO NOT:
- Break desktop navigation (only show on mobile)
- Forget overlay click to close
- Use complex animation libraries (Vue Transition component sufficient)

RESEARCH NOTE: Per CONTEXT.md, hamburger menu with slide-out drawer.
  </action>
  <verify>
grep -n "HamburgerMenu\|translate-x-full\|md:hidden" resources/js/Components/Mobile/HamburgerMenu.vue resources/js/Layouts/AuthenticatedLayout.vue
  </verify>
  <done>
HamburgerMenu component exists with slide-out drawer, overlay, and close-on-click-outside behavior.
  </done>
</task>

<task type="auto">
  <name>Task 3: Modify CardTooltip for mobile modal behavior</name>
  <files>resources/js/Components/CardTooltip.vue</files>
  <action>
Modify CardTooltip.vue to use modal-style behavior on mobile:

1. **Add mobile detection and modal state:**

```javascript
import { ref, computed, onMounted, onBeforeUnmount, watch } from 'vue'

// ... existing props

const isMobile = ref(false)
const showModal = ref(false)

// Detect mobile
const checkMobile = () => {
    isMobile.value = window.innerWidth < 768
}

onMounted(() => {
    checkMobile()
    window.addEventListener('resize', checkMobile)
    document.addEventListener('keydown', handleEscape)
})

onBeforeUnmount(() => {
    window.removeEventListener('resize', checkMobile)
    document.removeEventListener('keydown', handleEscape)
})

// Watch for card changes to show modal on mobile
watch(() => props.show, (newVal) => {
    if (newVal && isMobile.value) {
        showModal.value = true
    }
})

function closeModal() {
    showModal.value = false
    emit('close')
}

function handleBackdropClick(e) {
    if (e.target === e.currentTarget) {
        closeModal()
    }
}
```

2. **Update template to have mobile modal:**

```vue
<template>
    <Teleport to="body">
        <!-- Desktop tooltip (existing behavior) -->
        <Transition
            enter-active-class="transition-opacity duration-200"
            enter-from-class="opacity-0"
            enter-to-class="opacity-100"
            leave-active-class="transition-opacity duration-150"
            leave-from-class="opacity-100"
            leave-to-class="opacity-0">
            <div v-if="show && card && !isMobile"
                 class="fixed z-50 pointer-events-none"
                 :style="calculatedPosition">
                <!-- Existing tooltip content -->
            </div>
        </Transition>

        <!-- Mobile modal -->
        <Transition
            enter-active-class="transition-opacity duration-200"
            enter-from-class="opacity-0"
            enter-to-class="opacity-100"
            leave-active-class="transition-opacity duration-150"
            leave-from-class="opacity-100"
            leave-to-class="opacity-0">
            <div v-if="showModal && card && isMobile"
                 class="fixed inset-0 z-50 flex items-center justify-center bg-black/70 p-4"
                 @click="handleBackdropClick">
                <div class="bg-white rounded-xl shadow-2xl max-w-sm w-full overflow-hidden">
                    <!-- Close button -->
                    <button
                        @click="closeModal"
                        class="absolute top-2 right-2 z-10 p-2 bg-black/50 rounded-full text-white hover:bg-black/70"
                        aria-label="Close">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                        </svg>
                    </button>

                    <!-- Card image -->
                    <img
                        :src="`https://art.hearthstonejson.com/v1/render/latest/enUS/256x/${card.id}.png`"
                        :alt="card.name"
                        class="w-full"
                    />

                    <!-- Card info -->
                    <div class="p-4 bg-gray-900 text-white">
                        <h3 class="text-lg font-semibold">{{ card.name }}</h3>
                        <div class="flex items-center justify-between mt-2 text-sm">
                            <span class="text-gray-300">{{ card.cardClass }}</span>
                            <span class="px-2 py-1 rounded text-xs font-medium"
                                  :class="{
                                    'bg-orange-500': card.rarity === 'LEGENDARY',
                                    'bg-purple-500': card.rarity === 'EPIC',
                                    'bg-blue-500': card.rarity === 'RARE',
                                    'bg-gray-500': ['FREE', 'COMMON'].includes(card.rarity)
                                  }">
                                {{ card.rarity }}
                            </span>
                        </div>
                    </div>
                </div>
            </div>
        </Transition>
    </Teleport>
</template>
```

DO NOT:
- Show both tooltip and modal simultaneously
- Break desktop tooltip behavior
- Forget escape key to close modal

RESEARCH NOTE: Per CONTEXT.md, tap-to-modal for card tooltips on mobile.
  </action>
  <verify>
grep -n "isMobile\|showModal\|handleBackdropClick\|bg-black/70" resources/js/Components/CardTooltip.vue
  </verify>
  <done>
CardTooltip has mobile modal behavior with tap-to-open, backdrop-dismiss, and desktop tooltip preserved.
  </done>
</task>

<task type="auto">
  <name>Task 4: Integrate mobile tabs into DeckBuilder and optimize performance</name>
  <files>
  resources/js/Pages/DeckBuilder.vue
  vite.config.js
</files>
  <action>
1. **Modify DeckBuilder.vue** for mobile tabs:

Add state:
```javascript
const activeTab = ref('cards')
```

Add MobileTabs import:
```javascript
import MobileTabs from '@/Components/Mobile/MobileTabs.vue'
```

Add tab visibility computed:
```javascript
const isMobile = computed(() => typeof window !== 'undefined' && window.innerWidth < 768)
```

Wrap sections in mobile tabs:
```vue
<!-- Desktop layout (existing) -->
<div class="max-w-7xl mx-auto px-4 py-6 hidden md:block">
    <!-- Existing desktop grid layout -->
</div>

<!-- Mobile tabbed layout -->
<div class="md:hidden min-h-screen pb-20">
    <!-- Tab content -->
    <div v-if="activeTab === 'cards'" class="px-4 py-4">
        <!-- Card search and filters -->
    </div>
    <div v-else-if="activeTab === 'deck'" class="px-4 py-4">
        <!-- Deck list -->
    </div>
    <div v-else-if="activeTab === 'analytics'" class="px-4 py-4">
        <!-- Deck stats -->
    </div>

    <!-- Mobile tabs -->
    <MobileTabs
        :active-tab="activeTab"
        @change-tab="activeTab = $event"
    />
</div>
```

2. **Add lazy loading to images** (CardGrid.vue):

Modify CardGrid.vue to add loading="lazy":
```vue
<img
    :src="card.imageUrl"
    :alt="card.name"
    loading="lazy"
    decoding="async"
    class="card-image"
/>
```

3. **Verify code splitting in vite.config.js:**

Ensure vite.config.js has:
```javascript
export default defineConfig({
    plugins: [
        laravel({
            input: ['resources/js/app.js'],
            refresh: true,
        }),
    ],
    // No special config needed - import.meta.glob in app.js handles code splitting
})
```

4. **Add responsive image srcset support** to CardTooltip:
```vue
<img
    :srcset="`
        https://art.hearthstonejson.com/v1/render/latest/enUS/256x/${card.id}.png 1x,
        https://art.hearthstonejson.com/v1/render/latest/enUS/512x/${card.id}.png 2x
    `"
    :src="`https://art.hearthstonejson.com/v1/render/latest/enUS/256x/${card.id}.png`"
    :alt="card.name"
    class="w-full"
/>
```

DO NOT:
- Duplicate desktop content in mobile tabs (use conditional rendering)
- Lazy load above-fold images (critical content should load immediately)
- Break existing desktop layout

RESEARCH NOTE: Per RESEARCH.md, code splitting already configured via import.meta.glob, lazy load below-fold images.
  </action>
  <verify>
grep -n "MobileTabs\|activeTab\|loading=\"lazy\"\|srcset" resources/js/Pages/DeckBuilder.vue resources/js/Components/CardGrid.vue
  </verify>
  <done>
DeckBuilder has mobile tabbed layout, images use lazy loading, srcset for responsive images, code splitting verified.
  </done>
</task>

</tasks>

<verification>
After completion, verify:
1. Mobile view shows tabbed navigation (Cards/Deck/Analytics) at bottom
2. Tapping tabs switches content view smoothly
3. Card tooltips show as modal on mobile (tap to open, tap outside to close)
4. Hamburger menu opens slide-out drawer on mobile
5. Overlay click closes hamburger menu
6. Images below fold have loading="lazy" attribute
7. Responsive images use srcset for different pixel densities
8. Desktop layout unchanged (mobile-only features)
</verification>

<success_criteria>
1. Application is fully responsive and functional on mobile devices (< 768px)
2. Mobile tabs provide native-app-like navigation experience
3. Card tooltips use modal interaction on mobile (tap vs hover)
4. Hamburger menu provides slide-out navigation drawer
5. Images use lazy loading to improve initial page load
6. Responsive srcset provides appropriate image sizes for device density
7. Code splitting ensures only needed JavaScript is loaded per page
8. Core Web Vitals remain balanced (LCP < 2.5s, CLS < 0.1)
</success_criteria>

<output>
After completion, create `.planning/phases/04-monetization-platform-polish/04-03-SUMMARY.md` with:
- Frontmatter including phase, plan, wave, duration, tech_stack.added
- Mobile responsive design details (breakpoints, tabbed navigation)
- Performance optimizations implemented (lazy loading, code splitting, srcset)
- Key files created/modified
- Mobile UX improvements documented
- Verification commands for next plans
- Any deviations from research or issues encountered
</output>
