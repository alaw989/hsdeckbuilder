---
phase: 03-deck-sharing-and-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/js/Composables/useDeckStorage.js
  - resources/js/Utils/deckStorage.js
  - resources/js/Components/SavedDecksModal.vue
  - resources/js/Pages/DeckBuilder.vue
  - package.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can save current deck to browser LocalStorage with a custom name"
    - "User can view list of previously saved decks in a modal"
    - "User can load a saved deck, which replaces current deck with saved deck"
    - "User can delete a saved deck from the list"
    - "Saved decks persist across page reloads and browser sessions"
  artifacts:
    - path: "resources/js/Composables/useDeckStorage.js"
      provides: "Vue 3 composable for reactive LocalStorage deck management"
      exports: ["useDeckStorage", "savedDecks", "saveDeck", "loadDeck", "deleteDeck"]
      min_lines: 50
    - path: "resources/js/Utils/deckStorage.js"
      provides: "Pure utility functions for deck serialization/deserialization"
      exports: ["serializeDeckForStorage", "deserializeDeckFromStorage", "generateDeckId"]
      min_lines: 30
    - path: "resources/js/Components/SavedDecksModal.vue"
      provides: "Modal component for viewing and managing saved decks"
      min_lines: 80
    - path: "resources/js/Pages/DeckBuilder.vue"
      provides: "Main deck builder with Save/Load Decks buttons"
      contains: "SavedDecksModal"
  key_links:
    - from: "resources/js/Composables/useDeckStorage.js"
      to: "@vueuse/core"
      via: "useLocalStorage import"
      pattern: "from ['\"]@vueuse/core['\"]"
    - from: "resources/js/Components/SavedDecksModal.vue"
      to: "resources/js/Composables/useDeckStorage.js"
      via: "useDeckStorage composable"
      pattern: "useDeckStorage"
    - from: "resources/js/Utils/deckStorage.js"
      to: "resources/js/Utils/deckCode.js"
      via: "exportDeckCode for deckCode field"
      pattern: "exportDeckCode"
---

<objective>
Create LocalStorage-based persistence for saving, loading, and deleting decks.

Purpose: Enable users to save their work locally without server-side storage or authentication. LocalStorage provides 5-10MB capacity (sufficient for hundreds of decks) and works offline.

Output: useDeckStorage composable with VueUse integration, deckStorage utility functions, SavedDecksModal component, and DeckBuilder integration.
</objective>

<execution_context>
@/home/deck/.claude/get-shit-done/workflows/execute-plan.md
@/home/deck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 2 Completion
@.planning/phases/02-deck-builder-mvp/02-01-SUMMARY.md
@.planning/phases/02-deck-builder-mvp/02-03-SUMMARY.md

# Existing Code to Integrate
@resources/js/Composables/useDeckBuilder.js
@resources/js/Utils/deckCode.js
@resources/js/Pages/DeckBuilder.vue

# Research (MANDATORY - honor these recommendations)
@.planning/phases/03-deck-sharing-and-persistence/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deckStorage utility functions</name>
  <files>resources/js/Utils/deckStorage.js</files>
  <action>
Create new file resources/js/Utils/deckStorage.js with pure utility functions:

1. generateDeckId() - Generate unique deck ID using timestamp + random string
   Format: "{timestamp}-{random9chars}"

2. serializeDeckForStorage(deckCards, deckClass, deckName) - Convert deck to storage format
   - Use exportDeckCode from deckCode.js to get deckCode (compact)
   - Return object: { id, name, class, deckCode, cards: deckCards, createdAt, updatedAt }
   - Store full card data for offline display (LocalStorage has 5-10MB capacity)

3. deserializeDeckFromStorage(savedDeck) - Convert storage format to useDeckBuilder format
   - Return: { class, cards, name } for loading into useDeckBuilder
   - Parse deckCode via importDeckCode to validate card data still exists

DO NOT duplicate deckCode.js functionality - import and reuse exportDeckCode/importDeckCode.

Error handling: Return { data: null, error: string } for consistency with existing utilities.
  </action>
  <verify>
grep -n "export.*serializeDeckForStorage\|export.*deserializeDeckFromStorage\|export.*generateDeckId" resources/js/Utils/deckStorage.js
  </verify>
  <done>
File exists with all three exported functions, uses exportDeckCode from deckCode.js, returns { data, error } objects.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create useDeckStorage composable with VueUse</name>
  <files>resources/js/Composables/useDeckStorage.js</files>
  <action>
Create new file resources/js/Composables/useDeckStorage.js:

1. Import useLocalStorage from @vueuse/core (already installed @14.2.0)
2. Import deckStorage utility functions

3. Use STORAGE_KEY = 'hs-saved-decks'

4. Create reactive savedDecks array:
   const savedDecks = useLocalStorage(STORAGE_KEY, [])

5. Export functions:
   - saveDeck(deckData) - Creates new deck object, pushes to savedDecks, returns deck ID
   - loadDeck(id) - Finds deck by ID, returns data via deserializeDeckFromStorage
   - deleteDeck(id) - Removes deck from array, returns true/false
   - updateDeck(id, updates) - Updates existing deck (for rename)

6. Export computed: deckCount

Follow Vue 3 Composition API patterns from useDeckBuilder.js.

Handle QuotaExceededError in saveDeck with try/catch, return { data: null, error: string }.

CRITICAL: Use useLocalStorage for SSR safety and cross-tab sync (per research).
  </action>
  <verify>
grep -n "useLocalStorage\|saveDeck\|loadDeck\|deleteDeck" resources/js/Composables/useDeckStorage.js
  </verify>
  <done>
Composable created with useLocalStorage integration, all CRUD operations, QuotaExceededError handling.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create SavedDecksModal component</name>
  <files>resources/js/Components/SavedDecksModal.vue</files>
  <action>
Create new file resources/js/Components/SavedDecksModal.vue:

Props:
- show: boolean (modal visibility)

Emits:
- load-deck: { id, class, cards, name }
- delete-deck: id

Template:
1. Fixed overlay modal (same pattern as DeckCodeImport/Export)
2. List of saved decks with: name, class, card count, date (createdAt formatted)
3. Each deck has "Load" and "Delete" buttons
4. Empty state when no saved decks

Script:
1. Import useDeckStorage composable
2. Display savedDecks in a list
3. formatDate helper for readable dates (e.g., "Feb 7, 2026")
4. emit('load-deck', deckData) on Load click
5. emit('delete-deck', id) on Delete click, then call deleteDeck(id)

Styling: Use Tailwind classes matching existing modal components (white bg, shadow, rounded, etc.).
  </action>
  <verify>
grep -n "SavedDecksModal\|useDeckStorage\|emit.*load-deck\|emit.*delete-deck" resources/js/Components/SavedDecksModal.vue
  </verify>
  <done>
Modal component renders saved deck list, emits load-deck and delete-deck events.
  </done>
</task>

<task type="auto">
  <name>Task 4: Integrate save/load into DeckBuilder</name>
  <files>resources/js/Pages/DeckBuilder.vue</files>
  <action>
Modify resources/js/Pages/DeckBuilder.vue:

1. Add state for saved decks modal: showSavedDecksModal = ref(false)
2. Add save deck handler function:
   - Prompt user for deck name (window.prompt is acceptable for MVP)
   - Call saveDeck with current deck data
   - Show success message
3. Add load deck handler function:
   - Call clearDeck() to empty current deck
   - Call loadDeck(id) to get saved deck data
   - Iterate through cards and call addCard for each
   - Update selectedClass
4. Add delete deck handler:
   - Call deleteDeck(id)
5. Add "Save Deck" button in header (next to Import/Export buttons)
6. Add "Load Decks" button in header
7. Include SavedDecksModal component at template bottom
8. Wire up modal visibility and event handlers

Deck name prompt: Use default "Untitled {Class} Deck" if user cancels or enters empty.

DO NOT add server-side persistence - LocalStorage only per research.
  </action>
  <verify>
grep -n "SavedDecksModal\|saveDeck\|loadDeck\|showSavedDecksModal" resources/js/Pages/DeckBuilder.vue
  </verify>
  <done>
DeckBuilder has Save/Load buttons, SavedDecksModal component wired up with handlers.
  </done>
</task>

</tasks>

<verification>
After completion, verify:

1. Click "Save Deck" -> prompt appears -> enter name -> deck saves
2. Reload page -> click "Load Decks" -> saved deck appears in list
3. Click "Load" on saved deck -> current deck replaced with saved deck
4. Click "Delete" on saved deck -> deck removed from list
5. Check browser DevTools Application -> Local Storage -> hs-saved-decks key exists
</verification>

<success_criteria>
1. Deck saves to LocalStorage and persists across page reloads
2. Saved decks modal displays all saved decks with name, class, count, date
3. Loading a saved deck replaces current deck with saved deck data
4. Deleting a saved deck removes it from LocalStorage
5. useDeckStorage composable uses VueUse useLocalStorage (SSR-safe, cross-tab sync)
</success_criteria>

<output>
After completion, create `.planning/phases/03-deck-sharing-and-persistence/03-01-SUMMARY.md`

Include in SUMMARY frontmatter:
- phase: 03-deck-sharing-and-persistence
- plan: 01
- subsystem: persistence
- tags: [vueuse, localstorage, composables, deck-storage]
- requires: [02-01 (useDeckBuilder), 02-03 (deckCode utilities)]
- provides: [useDeckStorage composable, SavedDecksModal, LocalStorage persistence]
- affects: [03-02 (URL sharing), 03-03 (clone deck)]
- tech-stack:
    added: []
    patterns: [useLocalStorage for reactive persistence, { data, error } return pattern]
- key-files: [all created/modified files from tasks]
- key-decisions: ["LocalStorage with VueUse for SSR-safe reactive persistence", "Store full card data for offline display", "No server-side persistence for MVP"]
- patterns-established: ["useLocalStorage composable pattern for browser storage"]
</output>
