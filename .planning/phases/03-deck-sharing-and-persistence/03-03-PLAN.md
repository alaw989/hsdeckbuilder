---
phase: 03-deck-sharing-and-persistence
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - resources/js/Components/ConfirmCloneModal.vue
  - resources/js/Pages/DeckBuilder.vue
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User viewing shared deck can click 'Clone to My Decks' button"
    - "Clone prompts for deck name with pre-filled suggestion"
    - "Cloning saves shared deck to LocalStorage via useDeckStorage"
    - "After clone, deck remains loaded and can be edited"
    - "User receives visual confirmation that deck was cloned successfully"
  artifacts:
    - path: "resources/js/Components/ConfirmCloneModal.vue"
      provides: "Modal component for confirming and naming deck clone"
      min_lines: 50
    - path: "resources/js/Pages/DeckBuilder.vue"
      provides: "Deck builder with clone functionality integrated"
      contains: "ConfirmCloneModal"
  key_links:
    - from: "resources/js/Pages/DeckBuilder.vue"
      to: "resources/js/Composables/useDeckStorage.js"
      via: "saveDeck function for cloning"
      pattern: "saveDeck"
    - from: "resources/js/Pages/DeckBuilder.vue"
      to: "resources/js/Components/ConfirmCloneModal.vue"
      via: "clone-modal event and emit"
      pattern: "showCloneModal\|@confirm-clone"
    - from: "resources/js/Components/ConfirmCloneModal.vue"
      to: "resources/js/Pages/DeckBuilder.vue"
      via: "confirm-clone emit with deck name"
      pattern: "emit.*confirm-clone"
---

<objective>
Add functionality to clone shared decks to LocalStorage.

Purpose: Enable users to save shared decks to their local collection. After viewing a shared deck, users can clone it to their saved decks with a custom name, making it available for future editing without the shared URL.

Output: ConfirmCloneModal component with deck naming, DeckBuilder integration with clone handler, and saved deck confirmation.
</objective>

<execution_context>
@/home/deck/.claude/get-shit-done/workflows/execute-plan.md
@/home/deck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 3 Dependencies
@.planning/phases/03-deck-sharing-and-persistence/03-01-SUMMARY.md
@.planning/phases/03-deck-sharing-and-persistence/03-02-SUMMARY.md

# Existing Code
@resources/js/Composables/useDeckStorage.js
@resources/js/Pages/DeckBuilder.vue
@resources/js/Utils/deckStorage.js

# Research (MANDATORY - honor these recommendations)
@.planning/phases/03-deck-sharing-and-persistence/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ConfirmCloneModal component</name>
  <files>resources/js/Components/ConfirmCloneModal.vue</files>
  <action>
Create new file resources/js/Components/ConfirmCloneModal.vue:

Props:
- show: boolean (modal visibility)
- suggestedName: string (default name for the deck)

Emits:
- close: when modal dismissed without cloning
- confirm: { name: string } when user confirms clone

Template:
1. Fixed overlay modal (same pattern as other modals)
2. Title: "Clone This Deck"
3. Description: "Save this deck to your collection"
4. TextInput for deck name (pre-filled with suggestedName)
5. "Cancel" button (emits close)
6. "Clone to My Decks" button (emits confirm with name)

Script:
1. Ref for deckName, initialized to suggestedName
2. Focus input on mount (using template ref)
3. Handle Enter key to submit
4. emit('confirm', { name: deckName.value }) on confirm

Styling:
- White bg, shadow, rounded
- Input: standard TextInput or styled input
- Buttons: SecondaryButton (Cancel), PrimaryButton (Clone)

DO NOT add additional metadata fields (name only for MVP).
  </action>
  <verify>
grep -n "ConfirmCloneModal\|@confirm\|suggestedName" resources/js/Components/ConfirmCloneModal.vue
  </verify>
  <done>
Modal component renders with name input, emits confirm event with deck name.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate clone functionality into DeckBuilder</name>
  <files>resources/js/Pages/DeckBuilder.vue</files>
  <action>
Modify resources/js/Pages/DeckBuilder.vue:

1. Add state: showCloneModal = ref(false), cloneSuggestedName = ref('')

2. Update shared deck banner (added in 03-02):
   - Change "Start New Deck" button to secondary
   - Add "Clone to My Decks" button (PrimaryButton, blue)

3. Add clone button handler:
   - Set cloneSuggestedName.value = "Clone of {Class} Deck"
   - Set showCloneModal.value = true

4. Add confirm clone handler function:
   - Use useDeckStorage composable (import if not already)
   - Call serializeDeckForStorage(deckCards, selectedClass, name)
   - Call saveDeck with serialized data
   - Show success message: "Deck cloned successfully!"
   - Clear sharedDeck.value (now editing local copy)
   - Close clone modal

5. Include ConfirmCloneModal component at template bottom:
   <ConfirmCloneModal
     :show="showCloneModal"
     :suggested-name="cloneSuggestedName"
     @close="showCloneModal = false"
     @confirm="handleConfirmClone"
   />

6. Import useDeckStorage if not already imported:
   import { useDeckStorage } from '@/Composables/useDeckStorage'

7. Destructure needed functions:
   const { saveDeck } = useDeckStorage()

Handle save errors: If saveDeck returns error, show alert with error message.

DO NOT change deck contents after clone - keep same cards loaded for immediate editing.
  </action>
  <verify>
grep -n "ConfirmCloneModal\|showCloneModal\|handleConfirmClone\|useDeckStorage" resources/js/Pages/DeckBuilder.vue
  </verify>
  <done>
DeckBuilder has "Clone to My Decks" button in shared banner, ConfirmCloneModal wired up, saves to LocalStorage.
  </done>
</task>

</tasks>

<verification>
After completion, verify:

1. Open shared deck URL (from 03-02 testing)
2. Blue "Viewing Shared Deck" banner appears
3. Click "Clone to My Decks" -> clone modal opens
4. Input is pre-filled with "Clone of {Class} Deck"
5. Edit name if desired, press Enter or click "Clone to My Decks"
6. Success message appears: "Deck cloned successfully!"
7. Click "Load Decks" -> cloned deck appears in saved decks list
8. Clone modal can be cancelled with Escape key, backdrop click, or Cancel button
9. After cloning, shared deck banner disappears (now editing local copy)
</verification>

<success_criteria>
1. "Clone to My Decks" button appears in shared deck banner
2. Clone modal opens with suggested deck name pre-filled
3. Confirming clone saves deck to LocalStorage via useDeckStorage
4. Success confirmation shows after clone completes
5. Cloned deck appears in saved decks list
6. After clone, shared deck banner clears (editing local copy)
7. Clone modal can be dismissed without cloning
</success_criteria>

<output>
After completion, create `.planning/phases/03-deck-sharing-and-persistence/03-03-SUMMARY.md`

Include in SUMMARY frontmatter:
- phase: 03-deck-sharing-and-persistence
- plan: 03
- subsystem: cloning
- tags: [clone, localstorage, deck-copying, shared-decks]
- requires: [03-01 (useDeckStorage), 03-02 (shared deck loading)]
- provides: [ConfirmCloneModal, clone to LocalStorage functionality]
- affects: []
- tech-stack:
    added: []
    patterns: [clone modal pattern, shared-to-local deck conversion]
- key-files: [all created/modified files from tasks]
- key-decisions: ["Clone saves to LocalStorage without requiring server", "Keep same deck loaded after clone for immediate editing"]
- patterns-established: ["Shared deck workflow: View -> Clone -> Edit"]
</output>
