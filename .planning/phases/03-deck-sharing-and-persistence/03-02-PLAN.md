---
phase: 03-deck-sharing-and-persistence
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - resources/js/Utils/deckShare.js
  - resources/js/Components/ShareDeckModal.vue
  - resources/js/Pages/DeckBuilder.vue
  - app/Http/Controllers/DeckBuilderController.php
  - routes/web.php
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can generate shareable URL for current deck via Share button"
    - "Share URL contains encoded deck code as query parameter (?deck=...)"
    - "User can copy share URL to clipboard"
    - "Opening a shared URL loads the deck automatically in the builder"
    - "Shared deck banner shows when viewing deck from shared URL"
  artifacts:
    - path: "resources/js/Utils/deckShare.js"
      provides: "Utility functions for generating and parsing share URLs"
      exports: ["generateShareUrl", "parseSharedDeckFromUrl"]
      min_lines: 30
    - path: "resources/js/Components/ShareDeckModal.vue"
      provides: "Modal component for generating and copying share URLs"
      min_lines: 60
    - path: "app/Http/Controllers/DeckBuilderController.php"
      provides: "Laravel controller with sharedDeckCode parameter validation"
      contains: "sharedDeckCode"
    - path: "resources/js/Pages/DeckBuilder.vue"
      provides: "Deck builder with Share button and shared deck handling"
      contains: "ShareDeckModal"
  key_links:
    - from: "resources/js/Utils/deckShare.js"
      to: "resources/js/Utils/deckCode.js"
      via: "exportDeckCode import for deck code generation"
      pattern: "exportDeckCode"
    - from: "resources/js/Pages/DeckBuilder.vue"
      to: "app/Http/Controllers/DeckBuilderController.php"
      via: "Inertia page prop sharedDeckCode"
      pattern: "sharedDeckCode"
    - from: "app/Http/Controllers/DeckBuilderController.php"
      to: "routes/web.php"
      via: "/deck-builder route"
      pattern: "/deck-builder"
---

<objective>
Implement URL-based deck sharing using query parameters with backend validation.

Purpose: Enable users to share decks via unique URLs without server-side storage. The deck code is encoded in the URL query parameter (?deck=...), validated server-side, and loaded automatically when the page opens.

Output: deckShare utility functions, ShareDeckModal component, Laravel controller with query parameter validation, and DeckBuilder integration with shared deck banner.
</objective>

<execution_context>
@/home/deck/.claude/get-shit-done/workflows/execute-plan.md
@/home/deck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 2 Completion
@.planning/phases/02-deck-builder-mvp/02-03-SUMMARY.md

# Phase 3 Dependency
@.planning/phases/03-deck-sharing-and-persistence/03-01-SUMMARY.md

# Existing Code
@resources/js/Utils/deckCode.js
@resources/js/Pages/DeckBuilder.vue
@app/Http/Controllers/DeckBuilderController.php
@routes/web.php

# Research (MANDATORY - honor these recommendations)
@.planning/phases/03-deck-sharing-and-persistence/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deckShare utility functions</name>
  <files>resources/js/Utils/deckShare.js</files>
  <action>
Create new file resources/js/Utils/deckShare.js:

1. Import exportDeckCode from deckCode.js

2. generateShareUrl(deckCards, deckClass, baseUrl = window.location.origin)
   - Call exportDeckCode(deckCards, deckClass) to get deckCode
   - If error: return { url: null, error }
   - Encode deckCode: encodeURIComponent(deckCode)
   - Return: { url: `${baseUrl}/deck-builder?deck=${encodedDeck}`, error: null }
   - Use 'wild' format for broadest compatibility

3. parseSharedDeckFromUrl()
   - Read URLSearchParams: new URLSearchParams(window.location.search)
   - Get 'deck' parameter
   - Decode: decodeURIComponent(deckCode)
   - Return: { deckCode: string | null, error: string | null }

DO NOT implement compression - deckstring is already 50-100 chars, well under URL limits.

DO NOT use hash fragments - query params enable server validation per research.
  </action>
  <verify>
grep -n "generateShareUrl\|parseSharedDeckFromUrl\|exportDeckCode" resources/js/Utils/deckShare.js
  </verify>
  <done>
File exists with generateShareUrl and parseSharedDeckFromUrl functions, uses exportDeckCode.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Laravel controller for shared deck validation</name>
  <files>app/Http/Controllers/DeckBuilderController.php</files>
  <action>
Modify app/Http/Controllers/DeckBuilderController.php:

1. Update index() method to accept Request parameter
2. Get deck query parameter: $deckCode = $request->query('deck')
3. If deckCode exists, validate using Laravel validation:
   - 'required|string|min:30|max:200'
   - 'regex:/^[A-Za-z0-9+\/=]+$/u' (base64 pattern)
4. If validation fails: redirect to route('deck-builder') without deck parameter
5. Pass sharedDeckCode to Inertia render:
   return Inertia::render('DeckBuilder', [
     'cards' => $cards,
     'initialClass' => 'NEUTRAL',
     'sharedDeckCode' => $deckCode ?: null
   ]);

Use inline validator or $request->validate() pattern.

DO NOT decode deck server-side - frontend handles decoding via importDeckCode.
  </action>
  <verify>
grep -n "sharedDeckCode\|validate\|query('deck')" app/Http/Controllers/DeckBuilderController.php
  </verify>
  <done>
Controller validates deck parameter, passes sharedDeckCode to frontend on success.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create ShareDeckModal component</name>
  <files>resources/js/Components/ShareDeckModal.vue</files>
  <action>
Create new file resources/js/Components/ShareDeckModal.vue:

Props:
- show: boolean (modal visibility)
- shareUrl: string (the URL to display)
- isDisabled: boolean (disable when deck invalid)

Emits:
- close: when modal dismissed

Template:
1. Fixed overlay modal (same pattern as DeckCodeExport)
2. Display share URL in textarea/input
3. "Copy URL" button
4. Clipboard copy via navigator.clipboard.writeText()
5. Success feedback (2-second timeout like DeckCodeExport)
6. Error handling if clipboard API fails

Script:
1. Ref for copied success state
2. copyToClipboard() function using Navigator clipboard API
3. emit('close') on backdrop click

Style: Match existing DeckCodeExport component styling.

DO NOT implement custom clipboard fallback - Navigator API is sufficient for modern browsers.
  </action>
  <verify>
grep -n "ShareDeckModal\|clipboard\|copyToClipboard" resources/js/Components/ShareDeckModal.vue
  </verify>
  <done>
Modal component displays URL, copies to clipboard with visual feedback.
  </done>
</task>

<task type="auto">
  <name>Task 4: Integrate share functionality into DeckBuilder</name>
  <files>resources/js/Pages/DeckBuilder.vue</files>
  <action>
Modify resources/js/Pages/DeckBuilder.vue:

1. Add props: sharedDeckCode: { type: String, default: null }
2. Add state: showShareModal = ref(false), shareUrl = ref('')
3. Add sharedDeck ref for tracking if viewing shared deck
4. Add computed shareUrlComputed using generateShareUrl from deckShare.js
5. Add onMounted hook:
   - If props.sharedDeckCode exists, call handleImportDeck(props.sharedDeckCode)
   - Set sharedDeck.value = { class, cards, deckCode: props.sharedDeckCode }
6. Add "Share Deck" button in header (disabled when !isValidDeck)
7. Add share button handler: set shareUrl, show share modal
8. Include ShareDeckModal component at template bottom
9. Add shared deck banner (below header, shows when sharedDeck exists):
   - Blue background banner with "Viewing Shared Deck" text
   - Shows deck class and card count
   - "Clear" button to dismiss and return to normal editing
   - DO NOT add Clone button yet (that's plan 03-03)

Shared deck banner:
- position: relative
- bg-blue-100 border-l-4 border-blue-500 p-4 mb-4
- Text: "Viewing a shared deck. Make changes or start fresh."
- Button: "Start New Deck" (clears sharedDeck and deck)

DO NOT automatically save shared deck to LocalStorage - user must explicitly save (plan 03-03).
  </action>
  <verify>
grep -n "ShareDeckModal\|sharedDeckCode\|generateShareUrl\|sharedDeck" resources/js/Pages/DeckBuilder.vue
  </verify>
  <done>
DeckBuilder has Share button, ShareDeckModal, shared deck loading on mount, shared deck banner.
  </done>
</task>

</tasks>

<verification>
After completion, verify:

1. Build valid deck (30 cards) -> "Share Deck" button enabled
2. Click "Share Deck" -> modal opens with URL
3. Click "Copy URL" -> URL copied to clipboard, success message shows
4. Paste URL in new browser tab -> deck loads automatically
5. Blue "Viewing Shared Deck" banner appears when opening shared URL
6. Backend validates: Try invalid deck code in URL -> redirects to clean deck-builder
7. Test XSS prevention: Try ?deck=<script>alert(1)</script> -> validation rejects
</verification>

<success_criteria>
1. Share button generates URL with ?deck= parameter containing encoded deck code
2. Copy to clipboard works with visual feedback
3. Opening shared URL loads deck automatically via onMounted handler
4. Shared deck banner shows when viewing from shared URL
5. Laravel backend validates deck parameter format before passing to frontend
6. Invalid deck codes are rejected with redirect to clean deck-builder page
</success_criteria>

<output>
After completion, create `.planning/phases/03-deck-sharing-and-persistence/03-02-SUMMARY.md`

Include in SUMMARY frontmatter:
- phase: 03-deck-sharing-and-persistence
- plan: 02
- subsystem: sharing
- tags: [url-sharing, query-parameters, backend-validation, deck-codes]
- requires: [03-01 (deck storage), 02-03 (deckCode utilities)]
- provides: [deckShare utilities, ShareDeckModal, URL parameter validation, shared deck banner]
- affects: [03-03 (clone deck)]
- tech-stack:
    added: []
    patterns: [query parameter sharing, Laravel validation for URL params, clipboard API]
- key-files: [all created/modified files from tasks]
- key-decisions: ["Query parameters over hash fragments for server validation", "Deckstring format for compact URLs (50-100 chars)", "Backend validation for XSS prevention"]
- patterns-established: ["Share URL pattern: ?deck= with encodeURIComponent", "Laravel validation for query parameters"]
</output>
